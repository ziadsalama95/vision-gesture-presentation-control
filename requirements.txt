# Pinned to the last opencv version that still supports NumPy 1.x, so it stays
# compatible with the TensorFlow 2.17 that MediaPipe transitively imports for
# its optional docstring decorators on Windows/Python 3.11.
numpy<2
opencv-python==4.10.0.84
opencv-contrib-python==4.10.0.84
mediapipe>=0.10.21
pyautogui>=0.9.54
pygetwindow>=0.0.9
